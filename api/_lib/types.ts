/**
 * @license
 * SPDX-License-Identifier: Apache-2.0
 */

// Type-only re-export — erased at compile time, never resolved at runtime.
export type { Alert, CryptoCoin, WeatherInfo, CurrencyPair, AQIMeasurement } from '../../src/types';
